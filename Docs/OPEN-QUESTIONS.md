# Open Questions

1. For the MVP, is a TrackGrade-local preset or snapshot acceptable if it reliably recalls the dynamic grade state, or do you want preset save to remain device-native even though `systemPreset` recall is not preserving the current dynamic grade values on firmware `3.0.0.24`?
