# Open Questions

1. The reference ColorBox at `172.29.14.51` is discoverable over Bonjour as `ColorBox-1SC001145`, but direct HTTP/TCP access from this machine is currently failing with `No route to host` / `host-unreach` on port 80. Can you verify that the ColorBox web/API interface is reachable from this Mac and that no device-side network restriction is blocking local HTTP access?
